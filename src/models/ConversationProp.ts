// no longer being used, just kept incase its needed in the future
export interface ConversationProp {
    created_at: string;
    id: number;
    sender_id: number;
    text: string;
    receiver_id: number;
}